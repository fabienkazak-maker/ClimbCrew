name: Temporary statistics workflow disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow temporaire neutralisé : les statistiques sont appliquées directement."
