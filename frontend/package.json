{
  "name": "climbcrew-frontend",
  "private": true,
  "version": "2.2.0-legacy-import-ready",
  "type": "module",
  "scripts": {
    "predev": "node scripts/prepare-startup-video.mjs && node scripts/prepare-deployment-version.mjs",
    "dev": "vite --host 0.0.0.0",
    "prebuild": "node scripts/prepare-startup-video.mjs && node scripts/prepare-deployment-version.mjs",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^6.1.1",
    "vite": "^8.3.0",
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
